# README file for instructions with maxbin2.py wrapper for maxbin

Input files required will be:
1. A .txt file showing locations to where assembly file is, along with output directory to write the output too and the abundance files.


